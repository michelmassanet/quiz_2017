

<ul>
    <% for (var i in tips) { %>
        <% var tip = tips[i]; %>

        <li>

            <% if (tip.accepted ||
                    (session.user && (session.user.isAdmin  || tip.AuthorId===session.user.id || (quizAuthorId && quizAuthorId === session.user.id)))) { %>

                <%= tip.text %>

            <% } %>

            <% if (!tip.accepted &&
                    (session.user && (session.user.isAdmin || tip.AuthorId===session.user.id || (quizAuthorId && quizAuthorId === session.user.id)))) { %>

                <a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>/accept?_method=PUT"><button>Aceptar</button></a>

            <% } %>

            <% if ((session.user && (session.user.isAdmin || tip.AuthorId===session.user.id ||(quizAuthorId && quizAuthorId === session.user.id)))) { %>

                <a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>?_method=DELETE"><button>Borrar</button></a>
 
            <% } %>
<small> <span id="autor">(by <%= tip.Author ? tip.Author.username: "Anómino" %>) </span></small>
        </li>
    <% } %>
</ul>
